# A4_MLOPS

This repository contains the MLOps assignment workflow setup for CI validation.

## CI Workflow

The GitHub Actions workflow is available at `.github/workflows/ml-pipeline.yml`.
It runs automated checks on `push` events for all branches except `main`.
